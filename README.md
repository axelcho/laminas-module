# Drop in for DoctrineModule for Laminas Framework

## Installation

This is drop in replacement for DoctrineModule. 

```sh
composer require axelcho/laminas-module
```

Then add `DoctrineModule` to your `config/application.config.php`


